"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/jwt.ts
var jwt_exports = {};
__export(jwt_exports, {
  signOauthState: () => signOauthState,
  signTempToken: () => signTempToken,
  signToken: () => signToken,
  verifyOauthState: () => verifyOauthState,
  verifyTempToken: () => verifyTempToken,
  verifyToken: () => verifyToken
});
module.exports = __toCommonJS(jwt_exports);
var import_jose = require("jose");
function getSecret() {
  const secret = process.env.JWT_SECRET;
  if (!secret) {
    throw new Error("JWT_SECRET environment variable is required");
  }
  return new TextEncoder().encode(secret);
}
async function signToken(payload) {
  return new import_jose.SignJWT(payload).setProtectedHeader({ alg: "HS256" }).setExpirationTime("7d").sign(getSecret());
}
async function verifyToken(token) {
  try {
    const { payload } = await (0, import_jose.jwtVerify)(token, getSecret(), { algorithms: ["HS256"] });
    return payload;
  } catch {
    return null;
  }
}
async function signTempToken(payload, expiresIn = "10m") {
  return new import_jose.SignJWT(payload).setProtectedHeader({ alg: "HS256" }).setExpirationTime(expiresIn).sign(getSecret());
}
async function verifyTempToken(token) {
  try {
    const { payload } = await (0, import_jose.jwtVerify)(token, getSecret());
    return payload;
  } catch {
    return null;
  }
}
async function signOauthState(payload) {
  return signTempToken(payload, "10m");
}
async function verifyOauthState(token) {
  return verifyTempToken(token);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  signOauthState,
  signTempToken,
  signToken,
  verifyOauthState,
  verifyTempToken,
  verifyToken
});
//# sourceMappingURL=jwt.js.map