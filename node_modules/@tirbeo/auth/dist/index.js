"use strict";
"use client";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.tsx
var src_exports = {};
__export(src_exports, {
  AuthProvider: () => AuthProvider,
  useAuth: () => useAuth
});
module.exports = __toCommonJS(src_exports);
var import_react = require("react");
var import_api_client = require("@tirbeo/api-client");
var import_jsx_runtime = require("react/jsx-runtime");
var AuthContext = (0, import_react.createContext)(void 0);
function AuthProvider({ children }) {
  const [user, setUser] = (0, import_react.useState)(null);
  const [isLoading, setIsLoading] = (0, import_react.useState)(true);
  (0, import_react.useEffect)(() => {
    const fetchUser = async () => {
      try {
        const data = await import_api_client.apiClient.get("/api/me");
        setUser(data);
      } catch {
        setUser(null);
      } finally {
        setIsLoading(false);
      }
    };
    fetchUser();
  }, []);
  const signIn = async (email, password) => {
    try {
      const { error } = await import_api_client.apiClient.post("/api/auth/login", { email, password });
      if (error) return { error };
      const data = await import_api_client.apiClient.get("/api/me");
      setUser(data);
      return { error: null };
    } catch (e) {
      return { error: e?.message || "Sign in failed" };
    }
  };
  const signInWithOAuth = async (provider) => {
    window.location.href = `/api/auth/oauth/authorize?provider=${provider}`;
  };
  const signUp = async (email, password, displayName) => {
    try {
      const { error } = await import_api_client.apiClient.post("/api/auth/signup", { email, password, displayName });
      if (error) return { error };
      const data = await import_api_client.apiClient.get("/api/me");
      setUser(data);
      return { error: null };
    } catch (e) {
      return { error: e?.message || "Sign up failed" };
    }
  };
  const signOut = async () => {
    await import_api_client.apiClient.post("/api/auth/logout");
    setUser(null);
  };
  const refreshSession = async () => {
    try {
      const data = await import_api_client.apiClient.get("/api/me");
      setUser(data);
    } catch {
      setUser(null);
    }
  };
  return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
    AuthContext.Provider,
    {
      value: {
        user,
        isLoading,
        isAuthenticated: !!user,
        signIn,
        signInWithOAuth,
        signUp,
        signOut,
        refreshSession
      },
      children
    }
  );
}
function useAuth() {
  const context = (0, import_react.useContext)(AuthContext);
  if (context === void 0) {
    throw new Error("useAuth must be used within an AuthProvider");
  }
  return context;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AuthProvider,
  useAuth
});
//# sourceMappingURL=index.js.map